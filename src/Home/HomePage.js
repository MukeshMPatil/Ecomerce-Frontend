import * as React from 'react';
function HomePage() {

  return (
   <h1>

   Be sure to use routing properly in App.js and put the navbar component inside the BrowserRouter. As an example, let's pretend that your app.js looks similar to:</h1>
  );
}
export default HomePage;